# Core GUI
customtkinter==5.2.0
CTkMessagebox==2.5

# PDF processing
PyMuPDF==1.22.5

# Sentence tokenisation
textblob-de==0.4.3

# Extractive summarisation
spacy==3.6.0
scikit-learn==1.3.0
networkx==3.1

# Sentiment analysis (XLM-RoBERTa via HuggingFace)
transformers==4.31.0

# Named Entity Recognition (Flair German model)
flair==0.12.2

# Post-install steps
# ---------------------------------------------------------
# SpaCy German model (must be run once after pip install):
#   python -m spacy download de_core_news_md
#
# Flair and HuggingFace models are downloaded automatically
# on first use (~400 MB + ~500 MB respectively).
