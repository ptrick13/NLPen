[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "--cov=nlpen --cov-report=xml --cov-report=term-missing"

[tool.coverage.run]
omit = [
    "nlpen/nlp/combined.py",
    "nlpen/pdf/highlighter.py",
    "app.py",
    "main.py",
]
